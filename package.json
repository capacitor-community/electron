{
  "name": "capacitor-electron-platform",
  "private": true,
  "scripts": {
    "publish": "npm run build-all && lerna publish",
    "publish:next-prerelease": "npm run build-all && lerna publish prerelease --preid next --dist-tag next --pre-dist-tag next",
    "publish:next": "npm run build-all && lerna publish --preid next --dist-tag next --pre-dist-tag next",
    "build-all": "lerna run build"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
