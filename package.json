{
  "name": "@capacitor-community/electron",
  "description": "Containing project for Capacitor community support for the Electron platform.",
  "private": true,
  "scripts": {
    "publish": "lerna run build && lerna publish --force-publish --skip-git && npm run publish-push",
    "publish-push": "bash scripts/deploy.sh"
  },
  "license": "MIT",
  "husky": {
    "hooks": {
      "pre-commit": "pretty-quick --staged"
    }
  },
  "directories": {
    "doc": "docs"
  },
  "author": "IT-MikeS",
  "dependencies": {
    "lerna": "^2.4.0"
  },
  "devDependencies": {
    "all-contributors-cli": "^6.16.0"
  }
}
