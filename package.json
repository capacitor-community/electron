{
  "name": "@capacitor-community/electron",
  "description": "Containing project for Capacitor community support for the Electron platform.",
  "private": true,
  "scripts": {
    "lerna-deploy": "npm run lerna-version && npm run lerna-publish && npm run generate-changelog",
    "lerna-version": "lerna version --force-publish --conventional-commits --no-changelog",
    "lerna-publish": "lerna publish from-git",
    "generate-changelog": "conventional-changelog -o CHANGELOG.md -r 0 --config changelog.config.js"
  },
  "license": "MIT",
  "husky": {
    "hooks": {
      "pre-commit": "pretty-quick --staged"
    }
  },
  "directories": {
    "doc": "docs"
  },
  "author": "IT-MikeS",
  "dependencies": {},
  "devDependencies": {
    "all-contributors-cli": "^6.16.0",
    "conventional-changelog-cli": "^2.0.34",
    "conventional-changelog-conventionalcommits": "^4.3.0",
    "lerna": "^3.22.1"
  }
}
