{
  "name": "capacitor-electron-platform",
  "private": true,
  "scripts": {
    "publish": "npm run build-all && lerna publish",
    "publish:next": "npm run build-all && lerna publish --canary --preid next --no-git-tag-version --no-push",
    "build-all": "lerna run build"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
